public static int main (string[] args) {
	// Output: ``azAZ09``
	print ('a'.to_string ());
	print ('z'.to_string ());
	print ('A'.to_string ());
	print ('Z'.to_string ());
	print ('0'.to_string ());
	print ('9'.to_string ());

	// Output: ````
	print (((char) 0).to_string ());

	// Output: ``<newline>``
	print ('\n'.to_string ());
	return 0;
}
